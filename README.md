# 2rism

A responsive travel landing page built with React and Vite.

## Run locally

```bash
npm install
npm run dev
```

Use `npm run build` for a production build and `npm run lint` to check the source.